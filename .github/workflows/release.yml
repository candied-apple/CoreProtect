# This workflow builds the project and creates a GitHub release
# It can be triggered manually from the Actions tab
# To use: Go to Actions > Build and Release > Run workflow
# Optionally specify a version, or leave empty to use the version from pom.xml
name: Build and Release
on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Release version (leave empty to use version from pom.xml)'
        required: false
        default: ''

jobs:
  build-and-release:
    name: Build and Create Release
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Cache Maven packages
        uses: actions/cache@v4
        with:
          path: ~/.m2
          key: ${{ runner.os }}-m2-${{ hashFiles('**/pom.xml') }}
          restore-keys: ${{ runner.os }}-m2

      - name: Extract version from pom.xml
        id: extract_version
        run: |
          if [ -n "${{ github.event.inputs.version }}" ]; then
            echo "VERSION=${{ github.event.inputs.version }}" >> $GITHUB_OUTPUT
          else
            VERSION=$(mvn help:evaluate -Dexpression=project.version -q -DforceStdout)
            echo "VERSION=$VERSION" >> $GITHUB_OUTPUT
          fi

      - name: Build with Maven
        run: mvn -B clean verify

      - name: Find JAR file
        id: find_jar
        run: |
          # Look for the main shaded JAR (built by maven-shade-plugin)
          # Pattern matches CoreProtect-{version}.jar (e.g., CoreProtect-23.0.jar)
          JAR_FILE=$(find target -maxdepth 1 -name "CoreProtect-[0-9]*.jar" -not -name "*-original.jar" | head -n 1)
          if [ -z "$JAR_FILE" ]; then
            echo "Error: No JAR file found in target directory"
            exit 1
          fi
          echo "JAR_FILE=$JAR_FILE" >> $GITHUB_OUTPUT
          echo "JAR_NAME=$(basename $JAR_FILE)" >> $GITHUB_OUTPUT

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ steps.extract_version.outputs.VERSION }}
          name: Release v${{ steps.extract_version.outputs.VERSION }}
          draft: false
          prerelease: false
          files: ${{ steps.find_jar.outputs.JAR_FILE }}
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
